import{COLS,ROWS,CELL,PADDING,CANVAS_W,CANVAS_H,CELL_NAMES,WALL,MIRROR_FWD,MIRROR_BACK,SUN_EMITTER,MOON_EMITTER,SUN_RECEPTOR,MOON_RECEPTOR,MODE_SUN,MODE_MOON}from'./constants.js';export function render(ctx,grid,mode,beams,won,hx,hy){ctx.clearRect(0,0,CANVAS_W,CANVAS_H);ctx.fillStyle='#0d0d22';ctx.fillRect(0,0,CANVAS_W,CANVAS_H);const ox=PADDING,oy=PADDING;for(let y=0;y<ROWS;y++)for(let x=0;x<COLS;x++){const px=ox+x*CELL,py=oy+y*CELL;ctx.fillStyle=(x+y)%2===0?'#0d0d22':'#11112a';if(x===hx&&y===hy){ctx.fillStyle='#1a2a3a'}ctx.fillRect(px,py,CELL,CELL);ctx.strokeStyle='#1a1a3a';ctx.lineWidth=.5;ctx.strokeRect(px,py,CELL,CELL);const cx=px+CELL/2,cy=py+CELL/2,c=grid[y][x];if(c===WALL){ctx.fillStyle='#18183a';ctx.fillRect(px+3,py+3,CELL-6,CELL-6);ctx.fillStyle='#2a2a55';ctx.beginPath();ctx.arc(cx,cy,7,0,Math.PI*2);ctx.fill()}else if(c===MIRROR_FWD||c===MIRROR_BACK){ctx.strokeStyle=won?'#ffd700':'#8888cc';ctx.lineWidth=3;ctx.beginPath();if(c===MIRROR_FWD){ctx.moveTo(px+8,py+CELL-8);ctx.lineTo(px+CELL-8,py+8)}else{ctx.moveTo(px+8,py+8);ctx.lineTo(px+CELL-8,py+CELL-8)}ctx.stroke()}else if(c===SUN_EMITTER){ctx.fillStyle='#ffd700';ctx.shadowColor='#ffd700';ctx.shadowBlur=mode===MODE_SUN?20:0;ctx.beginPath();ctx.arc(cx,cy,7,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0}else if(c===MOON_EMITTER){ctx.fillStyle='#88bbff';ctx.shadowColor='#88bbff';ctx.shadowBlur=mode===MODE_MOON?20:0;ctx.beginPath();ctx.arc(cx,cy,7,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0}else if(c===SUN_RECEPTOR){ctx.fillStyle='#ffd700';ctx.beginPath();ctx.arc(cx,cy,5,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#ffd700';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(cx,cy,9,0,Math.PI*2);ctx.stroke()}else if(c===MOON_RECEPTOR){ctx.fillStyle='#88bbff';ctx.beginPath();ctx.arc(cx,cy,5,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#88bbff';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(cx,cy,9,0,Math.PI*2);ctx.stroke()}}const bc=mode===MODE_SUN?'#ffd700':'#88bbff';for(const b of beams){if(b.cells.length<1)continue;ctx.beginPath();ctx.moveTo(ox+b.emitter.x*CELL+CELL/2,oy+b.emitter.y*CELL+CELL/2);for(const c of b.cells)ctx.lineTo(ox+c.x*CELL+CELL/2,oy+c.y*CELL+CELL/2);ctx.strokeStyle=bc;ctx.globalAlpha=.35;ctx.lineWidth=3;ctx.shadowColor=bc;ctx.shadowBlur=12;ctx.stroke();ctx.shadowBlur=0;ctx.globalAlpha=1}}export function renderMini(ctx,grid){const s=CELL/2,ox=0,oy=0;ctx.clearRect(0,0,480,480);ctx.fillStyle='#0d0d22';ctx.fillRect(0,0,480,480);for(let y=0;y<ROWS;y++)for(let x=0;x<COLS;x++){const px=x*s,py=y*s;ctx.fillStyle=(x+y)%2===0?'#0d0d22':'#11112a';ctx.fillRect(px,py,s,s);ctx.strokeStyle='#1a1a3a';ctx.lineWidth=.5;ctx.strokeRect(px,py,s,s);const cx=px+s/2,cy=py+s/2,c=grid[y][x];if(c===WALL){ctx.fillStyle='#18183a';ctx.fillRect(px+1,py+1,s-2,s-2);ctx.fillStyle='#2a2a55';ctx.beginPath();ctx.arc(cx,cy,4,0,Math.PI*2);ctx.fill()}else if(c===MIRROR_FWD||c===MIRROR_BACK){ctx.strokeStyle='#8888cc';ctx.lineWidth=2;ctx.beginPath();if(c===MIRROR_FWD){ctx.moveTo(px+3,py+s-3);ctx.lineTo(px+s-3,py+3)}else{ctx.moveTo(px+3,py+3);ctx.lineTo(px+s-3,py+s-3)}ctx.stroke()}else if(c===SUN_EMITTER||c===SUN_RECEPTOR){ctx.fillStyle='#ffd700';ctx.beginPath();ctx.arc(cx,cy,4,0,Math.PI*2);ctx.fill()}else if(c===MOON_EMITTER||c===MOON_RECEPTOR){ctx.fillStyle='#88bbff';ctx.beginPath();ctx.arc(cx,cy,4,0,Math.PI*2);ctx.fill()}}}
